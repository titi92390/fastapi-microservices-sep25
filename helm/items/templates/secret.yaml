apiVersion: v1
kind: Secret
metadata:
  name: {{ include "items.fullname" . }}-secret
  labels:
    {{ include "items.labels" . | nindent 4 }}
type: Opaque
stringData:
  SECRET_KEY: "my-secret-key-for-dev"
