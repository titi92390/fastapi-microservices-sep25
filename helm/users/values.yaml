replicaCount: 1

image:
  repository: titi92390/fastapi-users
  tag: latest
  pullPolicy: Always

service:
  type: ClusterIP
  port: 8000

env:
  APP_ENV: dev

resources: {}

ingress:
  enabled: false

# 🔴 OBLIGATOIRE — LU PAR LE TEMPLATE
global:
  waitForDatabase: false
