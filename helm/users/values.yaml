replicaCount: 1

image:
  repository: leogrv22/users
  tag: dev
  pullPolicy: Always

service:
  type: ClusterIP
  port: 8000

env:
  APP_ENV: dev

resources: {}

ingress:
  enabled: false

# 🔴 OBLIGATOIRE — LU PAR LE TEMPLATE
global:
  waitForDatabase: false
